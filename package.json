{ 
  "name": "studwiki",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run start --prefix server\" \"npm start --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "install": "^0.13.0"
  }
  
}
